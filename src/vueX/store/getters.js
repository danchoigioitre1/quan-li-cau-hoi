export const getters = {
  name: (state) => state.name,
  exerciseMode: (state) => state.exerciseMode,
  exerciseList: (state) => state.exerciseList,
  loading: (state) => state.loading,
  gradeList: (state) => state.gradeList,
  subjectList: (state) => state.subjectList,
  questions: (state) => state.questions,
  exerciseSelectedId: (state) => state.exerciseSelectedId,
  questionSelected: (state) => state.questionSelected,
  questionMode: (state) => state.questionMode,
  exerciseSelected: (state) => state.exerciseSelected,
};
